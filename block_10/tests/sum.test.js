const sum = (a, b) => a + b;

describe('ay',()=>{
  test('sums two numbers', () => {
    expect(sum(2, 3)).toEqual(5);
  });
})

describe('add',()=>{
  test('sums two YEAAAAAAAAAAAAAAAAAAAAH', () => {
    expect(sum(2, 3)).toEqual(5);
  });
})
