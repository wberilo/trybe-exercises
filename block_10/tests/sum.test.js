const sum = (a, b) => a + b;

describe('case1',()=>{
  test('sums two numbers', () => {
    expect(sum(2, 3)).toEqual(5);
  });
})

describe('add',()=>{
  test('sums two numbers', () => {
    expect(sum(2, 3)).toEqual(5);
  });
  test('sums two numbers', () => {
    expect(sum(2, 3)).toEqual(5);
  });
  test('sums two numbers', () => {
    expect(sum(2, 3)).toEqual(5);
  });
  test('sums two numbers', () => {
    expect(sum(2, 455)).toEqual(5);
  });
})
